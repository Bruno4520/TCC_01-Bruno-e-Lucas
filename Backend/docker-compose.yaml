services:
  db:
    image: postgres:15.3
    volumes:
      - ./volumes/postgres/data:/var/lib/postgresql/data
    environment:
      POSTGRES_PASSWORD: tccsist
      POSTGRES_USER: tccsist
      POSTGRES_DB: tccsist
    ports:
      - "5432:5432"
